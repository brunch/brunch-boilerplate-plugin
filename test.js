'use strict';

const {expect} = require('chai');
const Plugin = require('.');

describe('Plugin', () => {
  let plugin;

  beforeEach(() => {
    plugin = new Plugin({
      plugins: {}
    });
  });
});

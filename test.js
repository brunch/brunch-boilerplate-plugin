'use strict';

const expect = require('chai').expect;
const Plugin = require('.');

describe('Plugin', () => {
  let plugin;

  beforeEach(() => {
    plugin = new Plugin({
      plugins: {}
    });
  });
});
